streamlit>=1.40.0
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
plotly>=5.18.0
requests>=2.28.0
MLB-StatsAPI>=1.7
pybaseball>=2.2.7
# Yahoo Fantasy API integration
# NOTE: yfpy and streamlit-oauth have conflicting python-dotenv pins
# and python-dotenv lacks Python 3.14 wheels. Install manually when resolved:
#   pip install yfpy>=17.0 --no-deps
#   pip install streamlit-oauth>=0.1.14 --no-deps
# The app works fully without these — Yahoo features degrade gracefully.
# Bayesian projection updates (optional — heavy dependency)
# pymc>=5.0
arviz>=0.15.0
# Lineup optimization
PuLP>=2.7
# News intelligence: RSS feed parsing
feedparser>=6.0
# Gap closure: web scraping for ADP/depth charts/contracts
beautifulsoup4>=4.12.0
# Gap closure: background refresh scheduler
# schedule>=1.2.0  # Optional — scheduler uses stdlib threading, no external dep needed
# Excel export (roster stats, cheat sheet)
openpyxl>=3.1.0
