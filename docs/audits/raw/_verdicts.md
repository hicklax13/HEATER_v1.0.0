# Phase C — adversarial verification verdicts

Each HIGH finding handed to an independent skeptic instructed to REFUTE it (default: not-a-finding unless proven). Severity/confidence here OVERRIDE the original auditor's where they differ.

| Finding | Verdict | Final severity | Conf | Notes from skeptic |
|---|---|---|---|---|
| DB-C1 (park-factor Tier 1 dead) | **CONFIRMED** | **Medium** (was High) | 92 | Mechanical bug fully verified: `team_batting()` returns a DataFrame, `isinstance(data,dict)` never True, no `.to_dict()`, emergency dict always served, Tier 1 dead. Severity downgraded: frozen values are curated 2026 FanGraphs 5-yr park factors (near-neutral, slow-drifting) → harm is "dead live tier / no current-season refresh," not corrupt data. Corrections: (1) refresh_log telemetry does NOT lie — line 634 logs `tier='emergency'` honestly (SF-22 fixed the telemetry, not the dead fetcher). (2) Extra live consumer found: `matchup_planner.py:309` reads the `park_factors` DB table — but it's also written with the emergency dict, so frozen too. |
| FA-C1 (sustainability sign inversion) | **CONFIRMED** | **High** (unchanged) | 95 | Verified xwoba_delta = xwOBA−wOBA 3 ways (database.py:1503/1505, alerts.py:520, test_xwoba_regression.py:43); `-23.0×` at waiver_wire.py:549 is genuinely inverted; primary weighted 2.0× (:587) so inversion dominates the combined logit. Guard test (test_sustainability_calibrated.py:87-106) fabricates the column under the OPPOSITE sign → "test encodes the bug." Pitcher (era-xfip) + BABIP branches correctly signed. High not Critical: sustainability is one multiplicative factor in the FA composite, not the SGP/grade authority. Fix (+23.0×) + production-convention guard test already proposed as FA-E2. |
| DB-C2 (closer monitor dead path) | **CONFIRMED** | **Medium** (was High) | 92 | Repo-wide grep: `closer_depth_data` has exactly ONE code occurrence (a READ at pages/3_Closer_Monitor.py:101), ZERO writes. Always falls to the SV heuristic (top-sv/team, confidence=sv/35, setup=[]). `build_closer_grid`'s real shape is produced only by `_persist_depth_chart_roles` → players.depth_chart_role (DB table), never the session key; depth_chart_role isn't even SELECTed into _build_player_pool. Downgraded: read-only advisory page; heuristic still surfaces the actual top-SV reliever (usually the real closer) with live SV/ERA/WHIP overlaid. Lost: committee detection, setup hierarchy, confidence nuance (SV double-counted since compute_job_security also weights sv). Page's "from FanGraphs depth chart" caption is misleading. |
| PV-C1 (stacking temporal mismatch) | **CONFIRMED** | **High** (unchanged) | 90 | Verified: projections CREATE TABLE (database.py:159-183) has NO season/forecast_season column; zero `ALTER TABLE projections ... season` migrations; `_load_stacking_weights` (database.py:1090) target=season_stats WHERE season=2025, features from projections with NO season filter, joined on player_id only → 2026 forecasts regressed onto 2025 actuals. data_pipeline.py:390-391 DELETE+reinsert leaves only current-year forecasts. **Skeptic checked the LIVE DB: 1,472 rows season=2025 + 9 non-blended systems → BOTH gates pass → leak path ACTIVE in production**, not the uniform fallback. Foundation every engine consumes. Harm is silent quality degradation (weights measure persistence not skill), not a crash. Fix = PV-E1 (forecast_season key + held-out training).

**Summary:** 4/4 HIGH findings CONFIRMED real, 0 false positives. Final: FA-C1 High, PV-C1 High, DB-C1 Medium, DB-C2 Medium. Adversarial pass also corrected 2 overstatements (refresh_log telemetry honest; severity calibration) — the report uses these adjusted severities.

## Phase D — report-conclusions review (independent)
Verdict: **publish with edits.** Spot-checked every Medium/Low the HIGH-skeptics didn't cover (MS-C1, MS-C2, DB-C3, FA-C2, DE-C1, DE-C2, DE-C3 + Lows) against current code — ALL confirmed at the cited file:line. No Known-Design-Choice collisions; no infeasible/redundant enhancements; executive framing ("core math sound, risk is silent degradation") defensible. **One edit applied:** MS-C2 has no production caller (`apply_time_decay` = definition + tests only) → dormant; downgraded Medium→Low for consistency with MS-C4. The MS-E2 enhancement stays (single-source season length is worthwhile regardless). Minor: confirmed TE-C1's file is `engine/output/weekly_matrix.py:355` (report citation correct).
