# B3 — FA recommender (subagent result)

**Verdict:** Most of the 22-PR overhaul's invariants hold + are well-guarded (marginal_sgp inverse signs; symmetric scarcity; roster floors; IL double-protection; punt 0.05×; multiplicative composite; NaN guard on add side). **Two methodology bugs remain — one HIGH.**

## Correctness findings
- **FA-C1** (L4, **HIGH**, conf 95) — *Hitter sustainability regression signal is SIGN-INVERTED vs the production xwoba_delta convention.* `src/waiver_wire.py:549` uses `primary_logit = -23.0 * xwoba_delta` with a docstring claiming pool stores `woba - xwoba`. But `src/database.py:1503` actually stores `xwoba_delta = xwOBA − woba_approx`. Corroborated 3×: database.py:1505 flags `xwoba_delta >= 0.030 → BUY_LOW`; alerts.py:519-549 labels negative delta "overperforming". So positive delta = underperforming = buy-low, but `-23.0×` drives sustainability DOWN for buy-low hitters and UP for sell-high hitters. FA composite multiplies by this inverted factor. **Fix:** `+23.0 * xwoba_delta`. The guard test (`test_sustainability_calibrated.py:87-106`) passes only because it fabricates `xwoba_delta=0.040` under the OPPOSITE sign — the exact "test encodes the bug" pattern PR #99 was meant to kill. (BABIP secondary `-8.0×(babip-baseline)` and pitcher `era-xfip` branch are correctly signed.) NOT a Known Design Choice.
- **FA-C2** (L4, **Medium**, conf 90) — *L14 (0.10) blend weight is dead in production.* `src/optimizer/fa_recommender.py:1090-1166` gates L14 on row cols `l14_pa/l14_ip` and reads `l14_<col>`, but those columns are NEVER populated on FAs (no `l14_*` writes in `_build_player_pool`/`data_bootstrap`). Recent form exists only as `ctx.recent_form[pid]` (a dict) and only for ROSTERED players, never FAs. So every FA row has `l14_active=False`, renormalizes to ROS+YTD, and canonical 0.70/0.20/0.10 silently degrades to ~0.78/0.22. Only activates in unit tests that hand-set the columns. NOT a Known Design Choice (CLAUDE.md asserts L14 "wired from l14_* columns" as a live invariant; prod data doesn't satisfy it).
- **FA-C3** (L4, **Low**, conf 70) — Explicit punt-tag path keys on `h2h_strategy['punt']` but producer populates `'punt_cats'` (`shared_data_layer.py:562`). `fa_recommender.py:686` hedges by checking both keys so not currently broken, but fragile; the `win_prob<0.10` fallback becomes the only reliable trigger. Confirm `weekly_h2h_strategy` key name.
- **FA-C4** (L4, **Low**, conf 60) — `compute_drop_cost` `is_hitter` cast not NaN-guarded. `waiver_wire.py:388` `int(row.get("is_hitter",0))` — add side uses `_is_hitter_safe()`; drop side doesn't. NaN `is_hitter` on a rostered row (DNA/partial-merge edge) would raise and abort the whole drop-scoring loop. Route through `_is_hitter_safe`.

## Enhancements (ranked)
1. **FA-E1** (High/Medium) — Wire a real L14 recent-form source into the FA blend (closes FA-C2). Extend `_load_recent_form` to fetch L14 for top-N FA candidate ids → `ctx.recent_form[fa_id]`; change `_blend_fa_row` to read from that dict (mirror `optimizer/projections.py:544/587`). Restores 0.70/0.20/0.10; catches role-change FAs 1-2 wks before ROS.
2. **FA-E2** (High/Low) — Add a production-convention guard test for sustainability sign (locks FA-C1 after fix). Derive `xwoba_delta` the way `_build_player_pool` does (xwOBA−wOBA), assert `compute_sustainability_score` agrees in direction with the row's own `regression_flag`.
3. **FA-E3** (Medium/Low) — Surface the playing-time double-penalty magnitude in the why-expander. `_scale_ros_by_playing_time` (0.20 floor) AND `_playing_time_multiplier` (0.30-1.0) stack to ~0.06× for 0-GP phantoms (correct for phantoms, but can suppress a legit just-promoted regular). Render combined PT factor; optionally relax to one gate for confirmed-everyday roles.
4. **FA-E4** (Low/Low) — Make ECR-stddev (`*=0.95/1.02` at fa_recommender.py:828-836) + regression nudge (1.05/0.95) registry-driven; last inline FA tunables (PR #107 centralized the rest).
