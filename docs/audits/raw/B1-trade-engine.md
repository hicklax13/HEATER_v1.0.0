# B1 — Trade engine (subagent result)

**Verdict:** No Critical/High correctness defects. All 9 documented load-bearing invariants upheld + test-guarded (inverse-cat signs via SGPCalculator; rate stats volume-weighted; MC overlay renames grade keys so it can't clobber Phase-1 (Bug A); paired-MC identical seeds + true antithetic u→1-u/z→-z; opponent_valuation requires sgp_denominators + per-cat clamp 3.0 (Bug C); ghost-team filter (Bug D); grade-range consistency (Bug E); IP-floor weeks_remaining horizon (Bug F); playoff invariants).

## Correctness findings
- **TE-C1** (L4, **Low**, conf 80) — Weekly matrix & playoff sim compute per-week means from the FULL roster (bench+IL), not LP-optimized starters. `src/engine/output/weekly_matrix.py:355` (`_per_week_means`), consumed `trade_evaluator.py:1638-1645` with before/after = full ~28-man roster. Observed: sums all rostered players, divides counting stats by 26 → overstates per-week counting means ~+30-50% and skews rate-stat volume weights toward bench/IL. Expected: use the 18 LP starters (the `_lineup_constrained_totals` path Phase-1 SGP uses), or at least exclude IL. Roughly symmetric across teams so it mostly cancels, but IL-stash-heavy rosters get distorted win-probs and mis-scaled deltas. NOT a Known Design Choice (bypasses the IL-de-weighting invariant).
- **TE-C2** (L4, **Low**, conf 70) — `classify_regime_simple` hardcodes league-avg xwOBA = 0.315 (stale; canonical 0.320 from CONSTANTS_REGISTRY). `src/engine/signals/regime.py:341`. Sibling `matchup.py:44-48` already fixed this exact drift; regime fallback was missed. 0.005 offset shifts level thresholds, can mis-bucket borderline players. Bounded impact (fallback feeding projection blend, not a direct grade input). NOT a Known Design Choice (residual D8 constant-drift).
- **TE-C3** (L4, **Low**, conf 90) — Dead computation: `_compute_other_teams_sgp` called for side-effects only; return discarded. `src/engine/monte_carlo/trade_simulator.py:118-119` (fn returns list at 452-470). MC evaluates trade as roster-vs-self SGP delta; opponent standings context never enters. Either remove (costs a full pass per MC) or actually use standings context. No grade impact (MC is risk diagnostic only). NOT a Known Design Choice (genuinely dead code).

## Enhancements (ranked)
1. **TE-E1** (impact High / effort Low) — Compute weekly-matrix & playoff-sim per-week means from LP starters (exclude IL), not full roster. Direct fix for TE-C1; playoff/champ delta is the engine's PRIMARY objective yet over-counts bench+IL. Reuse `_lineup_constrained_totals`. Expected: removes ~30-50% counting inflation, sharply better calibration for IL-stash rosters, lower Brier in backtest_calibration.
2. **TE-E2** (Medium/Medium) — Replace independent per-week category outcomes with copula-correlated weekly draws in playoff sim. `_prob_majority_cat_wins` (playoff_sim.py:504-520) uses Poisson-binomial NORMAL approx assuming the 12 cats independent; HR/RBI/R and ERA/WHIP/K move together (engine already owns DEFAULT_CORRELATION in copula.py). Independence understates variance → over-confident playoff/champ probs. Use existing GaussianCopula Cholesky.
3. **TE-E3** (Medium/Medium) — Simulate opponents' actual schedules by default (promote `full_league_schedule` Path A) instead of Binomial league-average (Path B, playoff_sim.py:194-198). Path A exists + tested; just opt-in. SoS variation determines top-4 seeding. Wire `load_league_schedule()` into the Trade Analyzer's evaluate_trade call.
4. **TE-E4** (Medium/Medium) — Empirically calibrate per-category weekly CV (weekly_matrix.py:58-75 `_DEFAULT_CV` is guessed not fit) and default-on Skellam for low-count counting cats (SB/SV/W/L). CV is the biggest lever on every win prob. Lowers cat_win_rmse (Section G target <8) + Brier.
5. **TE-E5** (Medium/Medium) — Model availability/injury inside the MC arms. `src/engine/context/injury_process.py` (Weibull duration + `sample_season_availability`) is built "to wire into trade_simulator" but `run_paired_monte_carlo` never calls it → CVaR5 downside understated for fragile/IL players. Sample availability with the same seeded rng (preserve paired-MC + antithetic), scale counting contributions. Gate behind `enable_injury_mc` (off for Trade Finder bulk, on for single-trade deep eval).
